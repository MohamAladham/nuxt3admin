import Swal, {SweetAlertResult} from 'sweetalert2'export default function () {    const error = (title = '', text = '') => {        fire({            title: title ? title : 'Error!',            text: text ? text : 'Something went wrong, please try again.',            icon: 'error',        })    }    const success = (title = '', text = '') => {        fire({            title: title ? title : 'Success!',            text: text ? text : 'Data has been saved successfully.',            icon: 'success',        })    }    const confirm = async (callBack, callbackParams: any = null, title = '', text = '', confirmText = '', cancelText = '') => {        return fire({            title: title ? title : 'Are you sure?',            text: text ? text : 'Do you really want to continue?',            icon: 'warning',            confirmButtonText: confirmText ? confirmText : 'OK',            showCancelButton: true,            cancelButtonText: cancelText ? cancelText : 'Cancel',        }).then((result) => {            if (result.isConfirmed) callBack(callbackParams);        })    }    const fire = (config): Promise<SweetAlertResult> => {        return Swal.fire({            title: '',            text: '',            icon: '',            confirmButtonText: 'OK',            showCancelButton: false,            cancelButtonText: 'Cancel',            ...config,        })    }    return {error, success, confirm, fire}}